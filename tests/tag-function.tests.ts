// function tagFunction(array: TemplateStringsArray, ...args: string[]) {
//   console.log(array);
//   console.log(args);
// }

it("tag function", () => {
  // const name = "Andry";
  // const day = "Morning";

  // tagFunction`Hello ${name}!, How are you ${day}?`;
  // tagFunction`Bye ${name}!, See you later?`;
});

it("tag function sql", () => {
  // const name = "Andry'; DROP TABLE users;";
  // const age = 20;

  // tagFunction`SELECT * FROM users WHERE name = ${name.toString()} AND age = ${age.toString()}`;
});
